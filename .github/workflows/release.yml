name: Release

on:
  workflow_dispatch:
    inputs:
      release-type:
        description: 'Release kind: prerelease | patch | minor | major'
        default: 'prerelease'
        required: true

permissions:
  contents: write
  pull-requests: read
  id-token: write

jobs:
  release:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - name: Compute next version
        id: version
        uses: vscheuber/version-bump-action@v1
        with:
          release-type: ${{ inputs.release-type }}

      - name: Update package manifests
        uses: vscheuber/manifest-version-update-action@v1
        with:
          version: ${{ steps.version.outputs.newVersion }}

      - name: Update changelog using AI action
        id: changelog
        uses: vscheuber/ai-changelog-action@v1
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          llm-api-key: ${{ secrets.LLM_API_KEY }}
          target-user-type: developer
          release-type: ${{ steps.version.outputs.action_release_type }}
          version: ${{ steps.version.outputs.newVersion }}
          commit: false

      - name: Write release notes from AI action output
        env:
          RELEASE_NOTES: ${{ steps.changelog.outputs.unreleased-content }}
        run: |
          set -euo pipefail
          printf '%s\n' "$RELEASE_NOTES" > RELEASE_NOTES.md

      - name: Promote release notes into versioned changelog section
        env:
          CHANGELOG_PATH: CHANGELOG.md
          RELEASE_TAG: ${{ steps.version.outputs.newTag }}
        run: |
          node - <<'NODE'
          const fs = require('fs');

          const changelogPath = process.env.CHANGELOG_PATH || 'CHANGELOG.md';
          const releaseTag = process.env.RELEASE_TAG;
          const date = new Date().toISOString().slice(0, 10);
          const releaseHeading = `## [${releaseTag}] - ${date}`;

          if (!fs.existsSync(changelogPath)) {
            throw new Error(`${changelogPath} does not exist`);
          }
          if (!fs.existsSync('RELEASE_NOTES.md')) {
            throw new Error('RELEASE_NOTES.md does not exist');
          }

          const notes = fs.readFileSync('RELEASE_NOTES.md', 'utf8').trim();
          if (!notes) {
            throw new Error('RELEASE_NOTES.md is empty');
          }

          const content = fs.readFileSync(changelogPath, 'utf8');
          const unreleased = content.match(/^##?\s+Unreleased\s*\n([\s\S]*?)(?=^##?\s+|(?![\s\S]))/im);

          if (!unreleased) {
            throw new Error('Could not find an Unreleased section in CHANGELOG.md');
          }

          if (content.includes(releaseHeading)) {
            // Rerun-safe: heading already exists, do not duplicate sections.
            process.exit(0);
          }

          const replacement = `## Unreleased\n\n${releaseHeading}\n\n${notes}\n\n`;
          const updated = content.replace(unreleased[0], replacement).replace(/\n{3,}/g, '\n\n');

          fs.writeFileSync(changelogPath, updated, 'utf8');
          NODE

      - name: Commit changelog and version
        run: |
          set -euo pipefail
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add CHANGELOG.md package.json
          if git diff --cached --quiet; then
            echo "No release changes to commit"
          else
            git commit -m "chore: release ${{ steps.version.outputs.newTag }}"
            git push origin HEAD
          fi

      - name: Create and push tag
        run: |
          set -euo pipefail
          git fetch --tags
          tag='${{ steps.version.outputs.newTag }}'
          if git rev-parse -q --verify "refs/tags/${tag}" >/dev/null; then
            existing_commit="$(git rev-list -n 1 "${tag}")"
            current_commit="$(git rev-parse HEAD)"
            if [[ "$existing_commit" == "$current_commit" ]]; then
              echo "Tag ${tag} already exists on the current commit; skipping"
              exit 0
            fi

            echo "Tag ${tag} already exists on a different commit"
            exit 1
          fi
          git tag -a "${tag}" -m "Release ${tag}"
          git push origin "${tag}"

      - name: Update major and minor action tags
        if: ${{ steps.version.outputs.preRelease == 'false' }}
        run: |
          set -euo pipefail
          version='${{ steps.version.outputs.newVersion }}'
          release_tag='${{ steps.version.outputs.newTag }}'

          IFS='.' read -r major minor patch <<< "$version"
          major_tag="v${major}"
          minor_tag="v${major}.${minor}"

          git tag -fa "$major_tag" -m "Update ${major_tag} to ${release_tag}"
          git tag -fa "$minor_tag" -m "Update ${minor_tag} to ${release_tag}"

          git push origin "refs/tags/${major_tag}" --force
          git push origin "refs/tags/${minor_tag}" --force

      - name: Validate publish action behavior (dry-run)
        uses: ./
        with:
          release-type: ${{ inputs.release-type }}
          package-name: npm-trusted-publish-action
          version: ${{ steps.version.outputs.newVersion }}
          dry-run: true

      - name: Publish GitHub release
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          RELEASE_TAG: ${{ steps.version.outputs.newTag }}
          IS_PRERELEASE: ${{ steps.version.outputs.preRelease }}
        run: |
          set -euo pipefail
          args=(release create "$RELEASE_TAG" --notes-file RELEASE_NOTES.md)
          if [[ "$IS_PRERELEASE" == "true" ]]; then
            args+=(--prerelease)
          fi
          gh "${args[@]}"
